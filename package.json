{
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init_auth_schema",
    "db:reset": "prisma migrate reset --force"
  },
  "devDependencies": {
    "vitest": "^3.1.4"
  }
}
